name: Publish image

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  packages: write
  checks: write

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish-container:
    name: Publish image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - run: docker build -t ghcr.io/mitchfen/nanoleaf-controller:latest .
        working-directory: src
      - run: docker push ghcr.io/mitchfen/nanoleaf-controller:latest 

  cleanup:
    needs: publish-container
    name: Cleanup untagged container images
    runs-on: ubuntu-latest
    steps:
      - uses: mitchfen/cleanup-untagged-container-images@main
        with:
          username: 'mitchfen'
          container-name: 'nanoleaf-controller'
          token: ${{ secrets.GHCR_TOKEN }}
