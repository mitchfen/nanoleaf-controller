apiVersion: v1
kind: Service
metadata:
  name: nanoleafcontroller-service
spec:
  type: ClusterIP 
  selector:
    app: nanoleafcontroller
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
